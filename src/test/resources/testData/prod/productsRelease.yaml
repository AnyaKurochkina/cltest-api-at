Astra:
  - options:
      - { platform: "OpenStack", env: "IFT" }

WildFly:
  - options:
      - { platform: "OpenStack", env: "HOTFIX" }

PostgreSQL:
  - options:
      - { platform: "OpenStack", env: "IFT" }

RabbitMQClusterAstra:
  - options:
      - { platform: "OpenStack", env: "LT" }

Windows:
  - options:
      - { platform: "OpenStack", env: "HOTFIX" }

Nginx:
  - options:
      - { platform: "OpenStack", env: "LT" }

Redis:
  - options:
      - { platform: "OpenStack", env: "PROD", segment: "prod-srv-app" }

ApacheKafkaCluster:
  - options:
      - { platform: "OpenStack", env: "HOTFIX" }

PostgresSQLCluster:
  - options:
      - { platform: "OpenStack", env: "LT" }

Artemis:
  - options:
      - { platform: "OpenStack", env: "PROD", segment: "prod-srv-app" }

