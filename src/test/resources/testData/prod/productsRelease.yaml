Astra:
  - options:
      - { platform: "OpenStack", env: "IFT" }

WildFly:
  - options:
      - { platform: "OpenStack", env: "HOTFIX" }

PostgreSQL:
  - options:
      - { platform: "OpenStack", env: "LT" }

RabbitMQCluster:
  - options:
      - { platform: "OpenStack", env: "IFT" }

Windows:
  - options:
      - { platform: "OpenStack", env: "HOTFIX" }

Nginx:
  - options:
      - { platform: "OpenStack", env: "LT" }

Redis:
  - options:
      - { platform: "OpenStack", env: "IFT"}

ApacheKafkaCluster:
  - options:
      - { platform: "OpenStack", env: "HOTFIX" }

PostgresSQLCluster:
  - options:
      - { platform: "OpenStack", env: "LT" }